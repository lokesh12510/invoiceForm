import { createTheme } from "@mui/material/styles";

export let DefaultTheme = createTheme({
  palette: {
    primary: {
      main: "#1976d2",
    },
    secondary: {
      main: "#2AC7A8",
    },
  },
});
