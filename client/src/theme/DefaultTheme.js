import { createTheme } from "@mui/material/styles";

export const DefaultTheme = createTheme({
  palette: {
    primary: {
      main: "#673ab7",
    },
    secondary: {
      main: "#2AC7A8",
    },
  },
});
